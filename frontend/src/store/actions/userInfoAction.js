import { USER_INFO_REQUEST, USER_INFO_SUCCESS, USER_INFO_ERROR } from './index';

import axios from 'axios';
import { config } from '../../config';

export const getPage = (path) => {
    return (dispatch) => {
        
    }
};